// Number bases and Logical Mathematics

export class ParseTo {
    public logTestFunction(e: string) {
        return e.toUpperCase()
    }
}

const parser = new ParseTo()

const testLog = parser.logTestFunction('teste')

console.log(testLog)
